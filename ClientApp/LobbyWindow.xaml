<Window x:Class="ClientApp.LobbyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lobby" Height="520" Width="820">
    <DockPanel Margin="12">

        
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
            <Button x:Name="RefreshBtn" Content="Refresh" Width="90" Height="30" Click="RefreshBtn_Click"/>
            <Button Content="Leave" Width="90" Height="30" Margin="8,0,0,0" Click="LeaveBtn_Click"/>
            <TextBlock x:Name="Status" Margin="12,4,0,0" Foreground="Gray"/>
        </StackPanel>

        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ListBox x:Name="ChatList" Grid.Row="0" Margin="0,0,0,8" />
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="110" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="MessageInput" Height="34" Margin="0,0,8,0"/>

                <Button Grid.Column="1"
                Content="Send"
                Width="100" Height="34"
                Click="SendMessage_Click"/>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
